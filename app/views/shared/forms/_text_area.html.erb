<%-
  options ||= {}
  options[:autocomplete] ||= "off"
  options[:rows] ||= 4
%>
<%= form.label field, label, class: "form-label" %>
<div class="mt-2">
  <%= form.text_area field, class: "form-input", **options %>
</div>